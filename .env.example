# Backend (copy to server/.env or set in hosting dashboard)

# Optional: comma-separated browser origins (scheme + host + port, no path). Required entries must match
# the SPA exactly, e.g. https://e-shop.bhabygroup.co.tz — or omit this variable entirely for open CORS.
# ALLOWED_ORIGINS=http://localhost:5173,https://e-shop.bhabygroup.co.tz

# MongoDB and other secrets — see server documentation
