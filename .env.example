# Backend (copy to server/.env or set in hosting dashboard)

# Optional: comma-separated browser origins allowed for CORS (omit for permissive / reflect-origin mode)
# ALLOWED_ORIGINS=http://localhost:5173,https://your-shop-domain.com

# MongoDB and other secrets — see server documentation
